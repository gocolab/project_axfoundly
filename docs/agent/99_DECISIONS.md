# 99. 결정 변경 이력

> 이미 ✅로 확정된 결정을 **변경**할 때만 기록한다. (최초 결정은 각 문서에 기록되므로 여기 쓰지 않는다)
> 목적: 나중에 "왜 이렇게 바꿨더라?"를 추적하고, AI가 과거에 폐기된 방안을 다시 제안하지 않게 한다.

## 변경 이력

> 최신 항목을 표의 맨 위에 추가한다.

| 날짜 | 문서 | 항목 | 이전 결정 | 새 결정 | 변경 이유 |
|---|---|---|---|---|---|
| 2026-08-11 | rules/05·05_AI | 얼굴 인식 모델 | YuNet+SFace 확정(2026-08-06), 정확도 근거 재검토 중 | **파이프라인 5종 확정** — 탐지 **YuNet**(Jetson 탑재) / 식별 앙상블 **ArcFace-R50 + AdaFace-ir101** / 정렬·포즈 **MediaPipe** / 정면화 **TP-GAN**(고도화·검증 후). 식별은 GPU에서, 탐지는 카메라(Jetson)에서. 결합 방식(score fusion vs voting)은 실측 비교 후 확정 | 08-11 전체 팀회의. 세션 분리·교차일 실측(08-10~11)에서 ArcFace-R50(94.9%)·AdaFace-ir101(94.9%·분리도 우위)이 상위 → 앙상블 채택. SFace는 고각 저하로 제외(상업 라이선스는 잃음). MediaPipe·TP-GAN은 식별자가 아니라 전처리 보조 |
| 2026-08-11 | rules/04·specs/12 | 개인정보 보존 방식 | TTL 인덱스로 고정 기간 후 자동 삭제 | **보존기간 = LMS `period`(과정 시작~종료일)** — 동의서 기한과 일치. LMS 응답의 `period`를 화이트리스트에 추가(08-07엔 "안 읽는 필드"로 제외했던 것), 종료일을 `expire_at`에 저장 → 기존 TTL 인덱스가 그 날짜에 삭제. period 형식 파싱은 실 LMS 연결 시 확정 | 08-11 팀회의. 고정 TTL은 수업 기간과 무관해 근거가 약했다. LMS가 과정 기간을 주므로 계산 없이 그대로 만료 시각으로 쓴다. "경찰 관제 로그식 전량 저장"은 데이터 과중으로 폐기 |
| 2026-08-11 | rules/06·specs/13 | 도커 컴포즈 배포 | 전 구성요소 compose로 학원 GPU 서버 배포(2026-08-07) | **compose 배포는 선택(체험용)** — 프로젝트 필수 아님. 원하는 사람만 `compose up`. Ollama+gemma4:26b는 compose에 올려둔 상태(PR #57)라 다음 주 LLM 작업에 사용 | 08-11 팀회의 + 실측: 배정 컨테이너(gpu_user16) 안에선 도커 데몬·GPU 권한이 없어 호스트 compose가 필요한데, 강사 방향을 "일부 무시, 우리 길로"로 정리. 배포 강제보다 개발 편의 우선 |
| 2026-08-10 | rules/05·specs/14 | LLM 용도·실행 방식·저장소 | LLM = RPA 단계 SMS 자동 발송 문구 생성 (모델·비용 미정, Spring notification에서 호출 예상) | **기능 5종으로 확정** (강사 Slack 제안 4종 반영) — ① 관제 자연어 어시스턴트(tool calling, 읽기 전용 도구 6종) ② 일일 이상 흐름 브리핑(JSON을 context window 직접 주입) ③ 양방향 가상 조교 — 답장 판독 후 **예외 '제안' 생성, 사람 승인 시에만 API-005 실행**(LLM 직접 호출 금지 — 문자 프롬프트 인젝션 차단) ④ 넛지 동적 메시지 — **골격 고정+LLM 변주+검증 폴백** 가드레일 ⑤ 예외 사유 확인 권장 플래그(허위 '판정' 아님). 실행 = 학원 GPU 서버 Ollama + webapp assistant 라우터. 저장소 = MongoDB 유지, **pgvector·Neo4j 모두 보류**(벡터 불요 — 강사 동일 방침). 모델 후보 gemma3·qwen3 — 툴콜+인젝션 실측 게이트 후 확정 | 강사 지시(8/10 목업 발표)·Slack 제안으로 확정. 출결 데이터는 정형이라 근거 확보는 RAG(벡터 검색)가 아닌 tool calling — 벡터 검색은 정답이 DB에 있는 질문에 근사치로 답해 오히려 할루시네이션을 늘린다. 자유 문구 생성·LLM 직접 쓰기는 검수 불가·인젝션 위험이라 가드레일·승인 흐름으로 수용. 전면 DB 전환(PostgreSQL)은 팀원 3도메인 재작성 5~7일로 8/31 일정과 양립 불가 |
| 2026-08-08 | rules/02·06 | 환경변수 관리 위치 | `database/.env`(DB 계정)와 리포 루트 `.env`를 따로 두고, Spring은 IDE Run Config에 환경변수를 직접 주입 | **리포 루트 `.env` 한 곳으로 통합** — Spring은 `spring.config.import: optional:file:../.env[.properties]`로 읽고, `database/apply.sh`·`seed.sh`는 루트 `.env`를 source한다. 중복이던 `database/docker-compose.yml`은 루트 compose의 `mongodb` 서비스로 대체 | 같은 값(특히 DB 비밀번호)이 여러 곳에 흩어져 있으면 한쪽만 고쳐 놓고 원인을 못 찾는 사고가 난다. 팀원이 IDE Run Config를 따로 설정해야 하는 것도 실질적인 진입 장벽이었다 |
| 2026-08-07 | rules/06·01 | 컨테이너화 범위 | **DB만 Docker** — 앱 서버(FastAPI·Spring·React)는 직접 실행 (2026-08-05 강사 지침, 학습 곡선 고려) | **전 구성요소 Docker Compose** — frontend(Nginx)·backend(Spring)·ai(FastAPI)·mongodb를 학원 GPU 서버 한 대에 올린다 | 강사가 판단을 3팀 CTO 협의로 위임했고, 협의 결과 전면 컨테이너화로 결정. 발표 당일 4개 서버를 각자 PC에서 순서 맞춰 띄우는 것보다 `compose up` 한 번이 사고 확률이 낮고, 시연 환경 재현성·포트폴리오 측면에서도 이득이 크다고 판단 |
| 2026-08-07 | specs/12·rules/04 | 미출석 수 집계 방식 | `attendees` 중 `checkInTime`이 없는 수를 우리가 직접 센다 | **LMS 강의 레벨 `absent`의 합을 쓴다** (화이트리스트에 `absent` 추가) | 실응답에 `status="조기취업"`인 수강생이 있다 — `checkInTime`이 null이라 우리는 미출석으로 세지만 LMS `absent`에는 포함되지 않아 집계가 어긋난다(실측 5 vs 4). 개인 `status`를 읽으면 구분되지만 화이트리스트 제외 필드이므로, 개인정보가 아닌 강의 레벨 집계를 쓰는 쪽을 택했다. 명단 저장에는 영향 없음 — 조기취업자도 미입실이라 어차피 저장되지 않는다 |
| 2026-08-07 | specs/12·rules/04 | 수업일(공휴일·휴강) 판단 | 요일 하드코딩 (`DETECTION_WEEKDAYS_ONLY`) — 평일이면 무조건 가동 | **LMS `classDay`(강의 레벨 bool)를 화이트리스트에 추가**, `false`인 강의는 퇴실 창 계산에서 제외. 요일 설정은 LMS 미응답 시의 2차 방어선으로만 유지 | 실응답 점검에서 발견한 필드 — 공휴일·휴강일을 우리가 달력으로 관리하지 않고 LMS가 알려주는 대로 따를 수 있다. 요일만 보는 기존 방식은 공휴일에도 카메라·배치가 도는 문제가 있었다 |
| 2026-08-07 | specs/12 | BAT-001 정합성 검증 항목 | `total` ↔ `attendees.length` 1쌍만 비교 | **LMS 집계 ↔ 우리 계산 5쌍 비교**로 확대 (`present+late`·`checkedOut`·`notCheckedOut`·`earlyLeave` 추가) | 실응답에서 `total` ↔ `attendees`가 6/6 일치해 기존 검증은 사실상 울리지 않는 반면, 나머지 집계를 우리 계산과 대조하면 **"우리 파이프라인이 제대로 세고 있나"** 를 매 tick 확인할 수 있다. 특히 `notCheckedOut`(관리 대상 수) 불일치는 문자 오발송과 직결된다 |
| 2026-08-07 | (팀 분담) | BAT-001 구현 지원 범위 | CTO가 담당 팀원과 페어 — Claude는 골격·체크포인트·리뷰만, 완성 코드는 만들지 않음 | **Claude가 attendance 도메인을 구현**하고 CTO가 리뷰 | 학습 기회 보존이라는 원래 취지는 담당자의 기존 커밋 이력을 살리고 수정 이유를 커밋 메시지에 남기는 것으로 대체한다 |
| 2026-08-07 | rules/04_DATABASE.md | 인덱스 관리 방식 | 각 서버 기동 시 코드로 ensure index (도메인별 구현) | **CTO 소유 `database/schema/` 스크립트로만 관리** — 앱은 데이터 CRUD만, 커스텀 롤로 DDL 차단(계정 3종 분리) | CTO 결정 — unique 인덱스가 명세의 계약(멱등 upsert·409·event_id 멱등)을 강제하고 TTL 인덱스가 개인정보 삭제의 백스톱이라, 도메인별 구현에 분산되면 누락 시 계약 위반·개인정보 잔존으로 이어진다. 또한 중복 데이터가 쌓인 뒤에는 unique 생성이 실패해 초기에 걸어야 한다 |
| 2026-08-07 | (팀 분담) | LMS 수집·적재(BAT-001·002·003) 구현 주체 | 팀원 attendance 담당이 구현 | **CTO가 담당 팀원과 페어로 진행** (Spring backend 위치는 유지) | CTO 결정 — 해당 팀원의 개발 숙련도를 고려해 CTO가 붙어서 함께 확인하며 진행. Claude는 완성 코드 대신 골격·체크포인트·리뷰로 지원해 팀원의 학습 기회를 남긴다 |
| 2026-08-07 | rules/05_AI.md | 얼굴 식별 정확도 근거 | 1차 실측 94.8%(임계 0.41) — "실용 충분"으로 SFace 선정 근거 중 하나 | **무효화** — 세션 분리 재측정에서 **27.1%**(오매칭 17.5%·미매칭 55.4%). 사용 가능한 임계값이 존재하지 않음. 툴(YuNet+SFace) 자체는 유지하되 정확도 항목은 재검토 | 1차 측정이 같은 영상의 연속 프레임을 등록/평가로 나눈 것이라 실제 운영 조건(다른 날 등록 → 다른 날 식별)을 반영하지 못했다. 원인은 데이터량이 아니라 촬영 조건(CCTV 하향 각도·저해상도)으로 보이며, 클래스 간 유사도(0.52)가 클래스 내 유사도(0.35)보다 큰 상태라 임계값 조정으로는 해결 불가 |
| 2026-08-06 | rules/00_PROJECT.md·specs/12 | 출결 판정 방식 | 출석/지각/결석 3분류 판정 + 예외처리 | **출석 여부 이진 판정** — LMS `check_in_time` 유무만 판단, 지각·유예(09:20~09:30) 분류 없음. 출석 마감(09:30)은 판정 기준이 아니라 **미출석 알림 트리거 설정값**으로만 사용(BAT-002). 예외(외출·조퇴·병결·오전/오후반)는 대시보드 수동 등록 유지 | CTO 결정 — 보조 시스템 포지셔닝: 공식 지각·유예 판정은 LMS 소관, 우리는 "태그 안 한 사람 찾기"에 집중. LMS와 이중 판정 시 불일치로 행정실 혼란 방지, 판정 로직 단순화 |
| 2026-08-05 | rules/08_COLLABORATION.md | 브랜치 전략·머지 권한 | GitHub Flow (main + feat + PR) | **main·dev 2단 + feat/*** — main 초기 상태로 리셋(기존 이력은 dev로 이관), feat→dev PR은 **CTO가 리뷰·머지 전담**, 마일스톤 시점 dev→main | CTO 결정 — main을 깨끗한 시연 기준선으로 유지, 품질 게이트 일원화 |
| 2026-08-05 | README·rules/00_PROJECT.md | 영상 소스 장비 | 상용 CCTV 3대 (RTSP) | **Jetson Nano + USB 웹캠 3세트** — 1차는 GStreamer RTSP 스트리밍 노드로 사용(하드웨어 H.264 인코딩), 엣지 추론 전환은 성능 확인 후 검토 | 학원 지침 — 장비가 Jetson Nano+웹캠으로 확정. "URL 하나로 읽는" 파이프라인 설계는 유지되어 코드 영향 없음 |
| 2026-08-05 | AGENT_GUIDE 개발 시작 조건 | 개발 게이트 예외 | rules 전체 ✅ 전 개발 금지 | **영상 파이프라인(webapp video·스트림·모니터링)은 선행 개발 허용** — 05_AI 보류 1건(얼굴 식별 방식)은 CTO 비교 테스트로 해소 중이며 해당 영역과 무관 | CTO 승인 — 강사 요구(모니터링 구조·스트림 구현, ~8/11) 대응 |
| 2026-08-05 | README·rules/00_PROJECT.md | CCTV 배치 | ①출결 시스템 앞 ②12층 엘베 2대 동시 감시 지점 ③1층 엘베 | **①②12층 엘베 2대 각각 입구 위 1대씩(정면 얼굴 확보) ③1층 엘베** | CTO 결정 — 출결 앞 카메라는 역할 불명확(사람-카드 매칭은 제외 범위)했고, 엘베 2대 동시 감시보다 입구 위 개별 설치가 얼굴 정면 확보에 유리 |
| 2026-08-05 | README·rules/00·02·05·07 | 문자 발송 조건·카메라 운영 | "LMS 태그 없음 AND 영상 미포착" 시에만 문자 | **"퇴실 미체크(DB 잔존) AND 퇴실 시간대 카메라 식별 포착" 시 RPA로 복귀 요청 문자** — 입실 시간대는 카메라 미사용(LMS 명단 DB 적재만), 카메라는 퇴실 시간대만 가동 | CTO 플로우 확정 — 시스템 목적이 "퇴실 미체크자 복귀 유도"로 구체화되며 조건 방향이 정반대로 정정됨 |
| 2026-08-05 | rules/02_BACKEND.md | FastAPI 아키텍처 | 도메인별 헥사고날 (Spring과 동일) | **FastAPI 표준 구조** (routers·schemas·services) — Spring은 헥사고날 유지 | 강사 지침(CTO-강사 논의) — webapp은 FastAPI 기본 디렉토리 관례를 따를 것 |
| 2026-08-05 | (리포 구조) | 루트 디렉토리 구성 | webapp/ 하위에 ai_server·backend·frontend·database 통합 | **webapp=FastAPI(AI 서버) 전용, backend·frontend·database는 루트 독립 폴더** | 강사 의도 확인(CTO-강사 논의) — webapp은 AI 서버 관리 전용 |
| 2026-08-05 | rules/01·04·06·07 | MongoDB 운영 위치 | CTO 개인 서버 Docker | **학원 GPU 서버 Docker** (SSH 수령 후 배포 — database/ 준비 완료, 팀원에겐 IP·포트·앱 계정 공유) | CTO 결정 — 상시 가동·팀 접근성. SSH는 학원 공유 대기 중 |
| 2026-08-05 | rules/01_TECH_STACK.md | DBMS | PostgreSQL 16 | **MongoDB 8.x** (CTO 서버 Docker, 팀원 IP 접속) | 8/5 회의 강사 확정 지침 — 단기간엔 JSON 바로 저장하는 NoSQL 적합. 8/4 기록한 충돌 해소 |
| 2026-08-05 | rules/01_TECH_STACK.md | 개발 OS | WSL2 + Ubuntu 24.04 | **Windows 유지** + Docker는 DB 용도로만 | 8/5 회의 강사 지침 — 학습 곡선 고려, 팀 전체 환경 통일 |
| 2026-08-05 | rules/01·02 | 탐지 모델 | YOLO 전제 | **얼굴 특화 툴 3종 비교 후 선정** (Face Detection·ArcFace·FaceNet) | 8/5 회의 — 3팀 CTO 분담 테스트·데이터 공동 활용. 선정 결과는 05_AI에 기록 |
| 2026-08-05 | rules/02_BACKEND.md | 문자 알림 경로 | Slack + 개인 문자 (경로 미상세) | Slack + **LMS 웹발신 API 문자** — LMS 태그·영상 모두 부재 시에만 발송 | 8/5 회의 — 판정은 LMS 기준(primary)·카메라 보조로 확정 |
| 2026-08-04 | (폴더 전체) | 문서 세트 용도 | 6인 취업 포트폴리오 사이드 프로젝트 | **학원 프로젝트3 "CCTV 출결 자동 관리 & 알림 시스템"** (2026-08-04~08-31) | 폴더 용도 전환 (사용자 요청). 이전 논의 잔재는 이 파일과 TOPIC_CANDIDATES.md 참고 |
| 2026-08-04 | rules/00_PROJECT.md | 최종 산출 목표 | 포트폴리오 (6인 취업 팀, 실배포+협업 증거) | 학원 팀 프로젝트 최종발표(8/31) + 포트폴리오 활용 | 폴더 용도 전환 |
| 2026-08-04 | rules/00_PROJECT.md | 개발 인원 | 협업 6명 (전원 주니어) | ❓ 리셋 — 학원 팀(4~6명, PM 체제), 인원·역할 확인 중 | 폴더 용도 전환 |
| 2026-07-31 | rules/00_PROJECT.md | 주 사용자/대상 | B2C 일반 소비자 | ❓ 미결정으로 리셋 | 주제 전면 재검토 (사용자 요청) |
| 2026-07-31 | rules/05_AI.md | AI가 하는 일 | MVP에 AI 미사용, 챗봇은 확장 | ❓ 미결정으로 리셋 | 주제 전면 재검토 (사용자 요청) |
| 2026-07-31 | TOPIC_CANDIDATES.md | 주제 후보 전체 (1~4차 50개, 24번 임차인 안전 진단 유력 검토, 앱 배포 방침 포함) | 24번을 유력안으로 심층 검토 완료 | 전면 리셋 | 사용자 요청 — 처음부터 다시 결정 |

## 폐기된 방안 (다시 제안 금지)

> 검토했지만 명확한 이유로 폐기한 방안. AI는 아래 방안을 다시 제안하지 않는다.

| 방안 | 폐기 이유 | 날짜 |
|---|---|---|
|  |  |  |
